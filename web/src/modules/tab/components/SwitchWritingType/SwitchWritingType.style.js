import styled from 'styled-components';
import theme from 'styles/theme';

export const SwitchWritingType = styled.div`
    position: relative;
    float: left;
    height: ${theme.subbar_height}px;
    padding: 6px 30px 0 0;
`;
